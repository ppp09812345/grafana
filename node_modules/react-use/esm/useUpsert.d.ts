import { Actions as ListActions } from './useList';
export interface Actions<T> extends ListActions<T> {
    upsert: (item: T) => void;
}
declare const useUpsert: <T>(comparisonFunction: (upsertedItem: T, existingItem: T) => boolean, initialList?: T[]) => [T[], Actions<T>];
export default useUpsert;
